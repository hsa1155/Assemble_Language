在我的程式中
首先我先配置好abc三矩陣的空間
並為ab矩陣給值
儲存方法就跟作業中figure要求c的儲存方法一樣

接下來我把r0固定為a[0][0]的位置
r1為b[0][0]
r2為c[0][0]

用r3來暫存內積運算各元素相乘的合

接下來用記憶體位置的加減來定位內積運算各元素的位置並計算
最後再把r3放回c矩陣內（r2的位置每次放就會自己移動到下一個位置）

重複這個步驟把c裡面每個元素都算完


編譯時 我參考範例makefile寫出自己的makefile 在終端機打make指令 就編譯完成

最後使用insight打開hw3.exe檔
執行後結果c裡面每一元素值都是正確的
